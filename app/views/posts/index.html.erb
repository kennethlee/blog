<% @posts.each do |post| %>
    <h4><td><%= link_to post.title, post_path(post) %></td></h4>
    <div class="date"><%= post.created_at.strftime("%B %d, %Y") %></div><br />
    <%= markdown(post.body.truncate(750)) %><%= link_to 'read more', post %><br />
<% end %>
